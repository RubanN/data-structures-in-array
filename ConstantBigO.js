// examples of constant time complexity ;
// It takes a whole number as an argument, and then just returns 2 times that number. So, if we pass in 5, then it will return 2 times 5, which is 10.
function myFunc(sum){
     return sum *2
}
// console.log(myFunc(3));
function getFirstName(name){
     return name.split(" ")[0]
}
// console.log(getFirstName("ruban"));

